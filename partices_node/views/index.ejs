<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NODE_JS  </title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <h4>hello Every one </h4>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="">
                <input class ='block w-full rounded-md outline-none px-5 py-2 bg-zinc-800' name ='title' type="text" placeholder="Title " >
                <textarea class ='block w-full rounded-md  outline-nresize-none px-5 py-2 bg-zinc-800' name = 'details' placeholder="Write your task details "></textarea>
                <button class="mt-2 bg-blue-500 px-3 py-4 rounded-md" type="submit">Add Task</button> 
            </form>
        </div>
        <div class="task flex p-5 gap-2 flex-wrap">
          <% if(files.length > 0 ){%>
            <%files.forEach(function(val){  %>
                <div class="task w-72 hidden px-3 py-4 bg-zinc-500 rounded-md ml-6"></div>
                <h3 class="text-White text-3xl tracking-tigher">Some Taks in There</h3>
                <a href="#" class="text-blue-500 inline-block  ">Read More......</a>
            </div>
          <%  }); %>
            <% } else {%>
                <h4 class="text-red-600">No Task yet </h4>
                <% }%>
                   
        </div>
    </div>
</body>
</html>