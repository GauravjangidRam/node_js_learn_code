<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Keyframe animation for continuous gradient movement */
        @keyframes gradientAnimation {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }

        /* Applying the animated gradient to the body */
        .bg-animated-gradient {
            background: linear-gradient(270deg, #4F46E5, #9333EA, #F59E0B, #10B981);
            background-size: 400% 400%;
            animation: gradientAnimation 15s ease infinite;
        }

        /* Applying the animated gradient to the box */
        .bg-box-gradient {
            background: linear-gradient(270deg, #1F2937, #4B5563, #111827, #374151);
            background-size: 400% 400%;
            animation: gradientAnimation 15s ease infinite;
        }
    </style>
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen bg-animated-gradient">

    <div class="bg-box-gradient shadow-2xl rounded-lg p-8 max-w-lg w-full">
        <h1 class="text-3xl font-bold text-gray-100 mb-4 border-b-2 pb-2">
            File Name: 
            <span class="text-indigo-400"><%= fileName %></span>
        </h1>

        <p class="text-gray-300 text-lg mb-6">
            <span class="font-semibold text-gray-100">Description:</span> 
            <span class="block bg-gray-800 p-4 rounded-md mt-2 shadow-inner leading-relaxed"><%= content %></span>
        </p>

        <a href="/" class="inline-block px-5 py-3 bg-indigo-600 text-white font-semibold rounded-lg shadow-lg transition-all duration-300 ease-in-out hover:bg-indigo-700 focus:outline-none focus:ring-4 focus:ring-indigo-500 focus:ring-opacity-50">
            Go Back
        </a>
    </div>

</body>
</html>
