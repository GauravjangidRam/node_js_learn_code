<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800 flex items-center justify-center h-screen">

    <div class="bg-zinc-900 p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-2xl font-bold text-white mb-6 text-center">Submit Your Info</h1>

        <form action="create" method="post" class="space-y-4">
            <!-- Name Input -->
            <div>
                <label class="block text-white text-sm mb-1">Name</label>
                <input type="text" name="name" placeholder="Enter your name" class="w-full px-4 py-2 rounded-lg border-2 border-zinc-700 focus:border-blue-500 focus:outline-none text-zinc-900">
            </div>

            <!-- Email Input -->
            <div>
                <label class="block text-white text-sm mb-1">Email</label>
                <input type="email" name="email" placeholder="Enter your email" class="w-full px-4 py-2 rounded-lg border-2 border-zinc-700 focus:border-blue-500 focus:outline-none text-zinc-900">
            </div>

            <!-- URL Input -->
            <div>
                <label class="block text-white text-sm mb-1">Website URL</label>
                <input type="url" name="url" placeholder="Enter your website URL" class="w-full px-4 py-2 rounded-lg border-2 border-zinc-700 focus:border-blue-500 focus:outline-none text-zinc-900">
            </div>

            <!-- Submit Button -->
            <div class="text-center">
                <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 transition duration-200">Submit</button>
            </div>
        </form>

        <!-- View Users Button (placed outside the form) -->
        <div class="text-center mt-4">
            <a href="/read" class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 transition duration-200">See Users</a>
        </div>
    </div>

</body>
</html>
