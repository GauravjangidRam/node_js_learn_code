<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
  <div class="w-full max-w-lg">
    <div class="bg-gray-800 rounded-lg shadow-lg p-6">
      <h2 class="text-2xl font-bold mb-4">Update User Info</h2>
      <!-- The form correctly wraps all elements and uses POST method -->
      <form action="/update/<%= user._id %>" method="POST" class="space-y-4">
        
        <div>
          <label for="name" class="block text-sm font-medium">Name</label>
          <input type="text" id="name" name="name" value="<%= user.name %>" 
                 class="w-full p-2 mt-1 bg-gray-700 text-white rounded focus:ring-2 focus:ring-indigo-500 focus:outline-none" required>
        </div>

        <div>
          <label for="email" class="block text-sm font-medium">Email</label>
          <input type="email" id="email" name="email" value="<%= user.email %>" 
                 class="w-full p-2 mt-1 bg-gray-700 text-white rounded focus:ring-2 focus:ring-indigo-500 focus:outline-none" required>
        </div>

        <div>
          <label for="url" class="block text-sm font-medium">Profile URL</label>
          <input type="url" id="url" name="url" value="<%= user.url %>" 
                 class="w-full p-2 mt-1 bg-gray-700 text-white rounded focus:ring-2 focus:ring-indigo-500 focus:outline-none">
        </div>

        <div class="text-center">
          <!-- The form submit button should be used to send data -->
          <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white py-2 px-4 rounded-lg font-semibold shadow-md focus:ring-2 focus:ring-indigo-500 focus:outline-none">
            Update User
          </button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
